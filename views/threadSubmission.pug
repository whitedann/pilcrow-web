extends layout

block content
  .container
    .row.my-5
      .col-md-6
        h1 The Story Title
        p #{completeString}
        form(method="POST")
          input.form-control(type='text', id='entry', placeholder='Type text here', name='entry')
          button.btn.btn-secondary.my-3(type="submit") Confirm
      .col-md-6
        .bg-light #{title}
