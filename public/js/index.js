import React from 'react';
import ReactDOM from 'react-dom';
import ThreadWidget from './thread_widget';

ReactDOM.render(React.createElement(ThreadsWidget, null), document.getElementById('reactEle'));